# Content Flow

A Vercel-ready multi-brand content calendar for **hustle.** and **The Second Studio**.

## Deploy

1. Upload this folder to a GitHub repository.
2. Import that repository into Vercel.
3. Keep the default Next.js build settings and deploy.

Each brand has its own content records. Data is saved in the current browser using local storage, so no environment variables or database setup are required.
